torch>=2.0.0            # PyTorch with CUDA support
transformers>=4.40.0    # HuggingFace transformers
accelerate>=0.30.0      # Model loading optimization
bitsandbytes>=0.43.0    # Quantization (4-bit, 8-bit)
                        # Used in: functions/LLM_predictions.py

# Data manipulation
pandas==2.2.3           # Data processing and CSV handling
                        # Used in: All prediction scripts, postprocessing scripts

# Progress bars
tqdm==4.67.1            # Progress bars for long-running predictions
                        # Used in: All prediction scripts

# Environment variables
python-dotenv>=1.0.0    # Load .env files for configuration
                        # Used in: benchmark scripts

# High-throughput inference (optional, for faster batched generation)
vllm>=0.6.0             # vLLM for optimized GPU inference
                        # Used in: functions/backends.py